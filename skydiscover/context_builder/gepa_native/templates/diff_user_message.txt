# Current Solution Information
- Main Metrics: 
{metrics}
- Focus areas: {improvement_areas}

# Program Generation History
## Previous Attempts

{previous_attempts}

{other_context_programs}

# Current Solution
{current_program}

{search_guidance}

# Task
Suggest improvements to the program that will improve its COMBINED_SCORE.
The system maintains diversity across these dimensions: score, complexity.
Different solutions with similar combined_score but different features are valuable.

## RESPONSE FORMAT — MANDATORY

You MUST output changes using SEARCH/REPLACE diff blocks. The parser is strict and will reject any other format.

**DO NOT** output plain code, prose descriptions of changes, or markdown fenced code blocks (```python ... ```) — these will be silently discarded and your response will fail.

**REQUIRED FORMAT** — every change must be wrapped exactly like this:

<<<<<<< SEARCH
<exact lines from the current solution, copied character-for-character>
=======
<replacement lines>
>>>>>>> REPLACE

Rules:
- The literal strings `<<<<<<< SEARCH`, `=======`, and `>>>>>>> REPLACE` must appear alone on their own lines.
- The SEARCH block must EXACTLY match the corresponding lines in "# Current Solution" — same whitespace, same indentation, same blank lines. Even one extra space will cause a mismatch.
- You may include multiple SEARCH/REPLACE blocks in a single response.
- Your explanation and reasoning can appear outside the blocks (before or between them).

**CORRECT example:**
<<<<<<< SEARCH
for i in range(m):
    for j in range(p):
        for k in range(n):
            C[i, j] += A[i, k] * B[k, j]
=======
for i in range(m):
    for k in range(n):
        for j in range(p):
            C[i, j] += A[i, k] * B[k, j]
>>>>>>> REPLACE

**WRONG — will be rejected:**
```python
for i in range(m):
    for k in range(n):
        ...
```

Be thoughtful about your changes and explain your reasoning thoroughly.
Include a concise docstring at the start of functions describing the exact approach taken.

IMPORTANT: If an instruction header of "## IMPORTANT: ..." is given below the "# Current Solution", you MUST follow it. Otherwise,
focus on targeted improvements of the program.

{timeout_warning}